# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  build:
    docker:
    - image: gcr.io/cloud-builders/bazel@sha256:43cb17c38ce6d9fa822137e71cc1e117550f33e42f4c6d778abe7b03826b606b
    steps:
    - checkout
    - run:
        command: bazel build //... --jobs=1
        name: Bazel Build
workflows:
  build:
    jobs:
    - build
