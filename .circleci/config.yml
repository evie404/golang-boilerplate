# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  build:
    docker:
    - image: gcr.io/cloud-builders/bazel@sha256:49d6a4713aa6b425ccfb7d08773a648658a3172343f27c4d926f2d161f545b59
    steps:
    - checkout
    - run:
        command: bazel build //... --jobs=1
        name: Bazel Build
workflows:
  build:
    jobs:
    - build
